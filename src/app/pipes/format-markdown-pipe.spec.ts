import { FormatMarkdownPipe } from './format-markdown-pipe';

describe('FormatMarkdownPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatMarkdownPipe();
    expect(pipe).toBeTruthy();
  });
});
